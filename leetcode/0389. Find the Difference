class Solution:
    def findTheDifference(self, s: str, t: str) -> str:
        counts = collections.Counter(s)
        countt = collections.Counter(t)
        res = list(countt - counts)
        return res[0]
